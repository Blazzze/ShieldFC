import { PadRightPipe } from './pipes/pad-right.pipe';

describe('PadRightPipe', () => {
  it('create an instance', () => {
    const pipe = new PadRightPipe();
    expect(pipe).toBeTruthy();
  });
});
